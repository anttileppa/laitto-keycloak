parent=keycloak.v2
import=common/keycloak

# Light overlay: keep keycloak.v2's PatternFly v5 base (styles.css + inherited stylesCommon),
# then layer our recolor on top via login.css.
styles=css/styles.css css/login.css
